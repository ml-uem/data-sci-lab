from setuptools import setup

setup(name='ml_uem',
      version='0.2',
      description='Machine Learning UEM Package',
      packages=['mluem'],
      install_requires=['numpy'],
      zip_safe=False)
